# Optional: AI commentary written by Claude (the game works without it).
# Frontend flag — tells the app to call /api/commentary
VITE_LLM_COMMENTARY=true
# Server-side only (Vercel → Project Settings → Environment Variables). Never prefix with VITE_.
ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-haiku-4-5
